export default {
    root: "three-dev",
    publicDir: "../3d-assets",
    build: {
    outDir: "../build",
    rollupOptions: {
        output: {
        },
    },
    chunkSizeWarningLimit: 1000,
}
};
